import Avatar from '.';
import { mount } from 'enzyme';
import React from 'react';

describe('Avatar', () => {
  describe('snapshot', () => {
    it('should match snapshot', () => {
      /* ...snapshot test... */
    });
  });

  describe('attributes', () => {
    it('should have X value', () => {
      /* ...attribute tests... */
    });
  });
});
