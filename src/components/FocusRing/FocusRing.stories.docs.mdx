The `<FocusRing />` component. This component is used as a pre-styled `<FocusRing />` from [React Aria - Focus](https://react-spectrum.adobe.com/react-aria/FocusRing.html). This component should be used for any input that needs a focus ring for WAI-ARIA.
