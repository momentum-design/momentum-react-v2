The `<TextArea />` component. This component is used is used to get the multi-line text input from the user.

Rendering TextArea from momentum-design (see more details here: https://momentum-design.github.io/momentum-design/storybook-static/index.html?path=/story/components-textarea--example)