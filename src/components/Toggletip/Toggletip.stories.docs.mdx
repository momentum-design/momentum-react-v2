The `<Toggletip />` component allows adding a Toggletip to whatever provided as `triggerComponent`. It
will show the Tooltip after a click event. But the Screen Reader user have to navigate manually to
the content of the popover, for example use Ctrl+Opt+RightArrow in VoiceOver.
