const CLASS_PREFIX = 'md-reaction';

const DEFAULTS = {
  SIZE: 12,
};

const SIZES = {
  12: 12,
  16: 16,
};

const STYLE = {
  wrapper: `${CLASS_PREFIX}-wrapper`,
};

export { CLASS_PREFIX, DEFAULTS, SIZES, STYLE };
