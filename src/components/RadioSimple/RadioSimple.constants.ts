const CLASS_PREFIX = 'md-radio-simple';

const DEFAULTS = {
  DISABLED: false,
};

const STYLE = {
  wrapper: `${CLASS_PREFIX}-wrapper`,
  focusRing: `${CLASS_PREFIX}-focusRing`,
  selected: `${CLASS_PREFIX}-selected`,
};

export { CLASS_PREFIX, DEFAULTS, STYLE };
