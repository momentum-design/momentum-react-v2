const CLASS_PREFIX = 'md-badge';

const DEFAULTS = {
  SIZE: 18,
};

const SIZES = {
  18: 18,
  12: 12,
};

const STYLE = {
  wrapper: `${CLASS_PREFIX}-wrapper`,
};

export { CLASS_PREFIX, DEFAULTS, SIZES, STYLE };
