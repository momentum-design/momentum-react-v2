import { default as ThemeProvider } from './ThemeProvider';
import * as CONSTANTS from './ThemeProvider.constants';
import { Props } from './ThemeProvider.types';

export { CONSTANTS as THEME_PROVIDER_CONSTANTS };

export type ThemeProviderProps = Props;

export default ThemeProvider;
