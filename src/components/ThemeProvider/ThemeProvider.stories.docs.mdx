The `<ThemeProvider />` component. This component is used to theme all child nodes within. This component primiarily utilizes **CSS Variables** for downstream components to consume and is completely independently stylable.