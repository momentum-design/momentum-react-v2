import Flex from '.';
import { mount } from 'enzyme';
import React from 'react';

describe('Flex', () => {
  describe('snapshot', () => {
    it('should match snapshot', () => {
      /* ...snapshot test... */
    });
  });

  describe('attributes', () => {
    it('should have X value', () => {
      /* ...attribute tests... */
    });
  });
});
