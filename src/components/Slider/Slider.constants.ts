const CLASS_PREFIX = 'md-slider';

const DEFAULTS = {};

const STYLE = {
  wrapper: `${CLASS_PREFIX}-wrapper`,
  track: `${CLASS_PREFIX}-track`,
  trackColored: `${CLASS_PREFIX}-track-colored`,
  trackDisabled: `${CLASS_PREFIX}-track-disabled`,
  thumb: `${CLASS_PREFIX}-thumb`,
  thumbDisabled: `${CLASS_PREFIX}-thumb-disabled`,
};

export { CLASS_PREFIX, DEFAULTS, STYLE };
