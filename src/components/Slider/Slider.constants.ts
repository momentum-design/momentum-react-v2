const CLASS_PREFIX = 'md-slider';

const DEFAULTS = {
  DISABLED: false,
  VALUE: 0,
  MIN: 0,
  MAX: 10,
  STEP: 1,
};

const STYLE = {
  wrapper: `${CLASS_PREFIX}-wrapper`,
};

export { CLASS_PREFIX, DEFAULTS, STYLE };
