The `<Tooltip />` component allows adding a Tooltip to whatever provided as `triggerComponent`. It
will show the Tooltip after a hover event. With `isToggletip` prop Tooltip appear when user click the trigger component.
