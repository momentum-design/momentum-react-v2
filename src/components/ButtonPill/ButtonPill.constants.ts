const CLASS_PREFIX = 'md-button-pill';

const DEFAULTS = {
  COLOR: 'primary',
  DISABLED: false,
  GHOST: false,
  OUTLINE: false,
  SHAPE: 'pill',
  SIZE: 1,
};

const COLORS = {
  JOIN: 'join',
  CANCEL: 'cancel',
  MESSAGE: 'message',
};

const STYLE = {
  wrapper: `${CLASS_PREFIX}-wrapper`,
};

export {
  COLORS,
  DEFAULTS,
  STYLE,
  CLASS_PREFIX,
};