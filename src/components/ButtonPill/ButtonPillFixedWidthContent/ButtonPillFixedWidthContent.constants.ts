const CLASS_PREFIX = 'md-button-pill-fixed-width-content';

const STYLE = {
  wrapper: `${CLASS_PREFIX}-wrapper`,
  buttonContent: `${CLASS_PREFIX}-button-content`,
  hidden: `${CLASS_PREFIX}-hidden`,
};

export { CLASS_PREFIX, STYLE };
