const CLASS_PREFIX = 'md-virtualized';

const DEFAULTS = {};

const STYLE = {
  container: `${CLASS_PREFIX}-container`,
  listWrapper: `${CLASS_PREFIX}-list-wrapper`,
};

export { CLASS_PREFIX, DEFAULTS, STYLE };
