The `<Tag />` component. This component, when the `format` prop is set to `normal`, will inherit colors and act as a button. When a format outside of `normal` is selected, it will act as a static element.