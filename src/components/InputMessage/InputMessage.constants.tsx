const CLASS_PREFIX = 'md-input-message';

const STYLE = {
  wrapper: `${CLASS_PREFIX}-wrapper`,
  message: `${CLASS_PREFIX}-message`,
  text: `${CLASS_PREFIX}--text`,
  icon: `${CLASS_PREFIX}--icon`,
};

const MESSAGE_LEVEL = ['success', 'warning', 'error', 'none'];

export { STYLE, MESSAGE_LEVEL };
