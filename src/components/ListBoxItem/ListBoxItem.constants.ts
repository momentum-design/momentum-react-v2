const CLASS_PREFIX = 'md-list-box-item';

const DEFAULTS = {};

const STYLE = {
  wrapper: `${CLASS_PREFIX}-wrapper`,
  contentsWrapper: `${CLASS_PREFIX}-contents-wrapper`,
  tickIcon: `${CLASS_PREFIX}-tick-icon`,
};

export { DEFAULTS, STYLE };
