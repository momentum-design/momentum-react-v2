The `<Chip />` component.

The component can display text, icons, and/or an avatar supplied by the consumer

The defaul size of the component is 28, the icon size as designed is 16, but can be set by consumer.

Both icons and avatar will be positioned within chip by default.

Padding of .25em overrides supplied style prop.
