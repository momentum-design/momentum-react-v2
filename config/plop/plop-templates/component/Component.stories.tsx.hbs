import React from 'react';
import {storiesOf} from '@storybook/react';

import { {{~componentName}},  Momentum{{componentName}}Props} from './';

storiesOf('{{componentName}}', module)
  .add(
    'name me',
    () => render({})
  );

const render = (props: Momentum{{componentName}}Props) => {
  return (
    <{{componentName}} {...props}/>
  );
}
