import React, { FC } from 'react';
import classnames from 'classnames';

import './{{componentName}}.style.scss';
import { Props } from './{{componentName}}.types';
import { DEFAULTS, STYLE } from './{{componentName}}.constants';

const {{componentName}}: FC<Props> = (props: Props) => {
  const { className } = props;

  // Implementation goes here
  return <div className={classnames(className, STYLE.wrapper)} />;
};

/**
 * TODO: Add description of component here.
 */

export default {{componentName}};
