import React, { FC } from 'react';
import classnames from 'classnames';

import { STYLE } from './{{componentName}}.constants';
import { Props } from './{{componentName}}.types';
import './{{componentName}}.style.scss';

/**
 * TODO: Component description.
 */
const {{componentName}}: FC<Props> = (props: Props) => {
  const { className, id, style } = props;

  // TODO: Implementation goes here.
  return <div className={classnames(className, STYLE.wrapper)} id={id} style={style} />;
};

export default {{componentName}};
